import React from "react";
import styles from '../common/mainStyles.module.scss'

export const Footer = () => {
    return (
        <div className={styles.footer}>
            Footer
        </div>
    )
}
